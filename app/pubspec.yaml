name: deep_links_example
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  go_router: ^14.2.2
  url_strategy: ^0.2.0
  get_it: ^7.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
